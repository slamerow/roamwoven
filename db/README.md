# Database Notes

`schema.sql` is the current draft schema. The next implementation step is to turn this into real Supabase migrations and wire:

1. Auth.
2. Trip creation.
3. Dashboard trip listing.
4. Upload records.
5. Published trip snapshots.

The prototype must keep building without Supabase env vars until real backend work begins.

